To create a folder for a day N:
```bash
npm run create -- N
```

To start run `dayN/script.js` on save:
```bash
npm run watch -- N
```

To init rust
```
cargo init --bin
cargo install cargo-watch
```

To watch with rust:
```bash
cargo watch -x run -c
```